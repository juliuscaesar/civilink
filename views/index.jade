extends layout

block content
  .row
    .col-sm-8
      br
      br
      h1 Welcome to CiviLink
        br 
        small a social platform for community engagement.
    .col-sm-4
      .row
        .content-box
          div
            p.text-center.login-title Sign in
              div.account-wall
                form(class='form-signin', action='/login', method='POST')
                  input(type='text', name='username' class='form-control', placeholder='Username',required, autofocus)
                  input(type='password', name='password' class='form-control', placeholder='Password', required)
                  button(class='btn btn-md btn-primary btn-block', type='submit') Sign in
                  span.clearfix
              #message
              if message
                br
                p.text-center.error-message #{message}
                br

              a(href='/sign-up', class='text-center') Create an account
              

  